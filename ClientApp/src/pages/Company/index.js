import React from 'react'
import { PublicMain, PublicMenuValues } from '../../layouts'

const Company = () => (
  <PublicMain menuValue={PublicMenuValues.Company}>
    <h1>Empresa</h1>
  </PublicMain>
)

export default Company
